# Rapport régression — v62
**Date** : 2026-07-01
**Stories** : STORY-15, STORY-16, STORY-17
**Verdict** : AUCUNE RÉGRESSION

---

## Checklist exécutée

| Item | Description | Résultat | Source de vérification |
|------|-------------|----------|----------------------|
| Coach — `renderSessions()` | Présente, signature inchangée | OK | coach-dashboard.js ligne 27 |
| Coach — `renderPlayers()` | Présente, signature inchangée | OK | coach-dashboard.js ligne 1485 |
| Coach — `showCoachRadar()` | Présente ; toolbar contient `btnExportPpt` avec `onclick="exportCoachPPT()"` | OK | coach-dashboard.js ligne 1049 |
| Coach — `exportCoachPPT()` | Présente et complète (lignes 551–802) ; 3 slides générés ; appelle `addAxisSlides` deux fois (lignes 788 et 791) | OK | coach-dashboard.js + QA-18 |
| Coach — `addAxisSlides()` | Présente en tant que fonction interne de `exportCoachPPT()` (ligne 695), appelée | OK | coach-dashboard.js lignes 695–785 |
| Coach — `exportCoachPDF` absente | Aucune occurrence dans coach-dashboard.js ni dans aucun fichier JS/HTML | OK | grep négatif + QA-19 cas 1 |
| Coach — `exportProgressionPPT` absente | Aucune occurrence dans coach-dashboard.js ni dans aucun fichier JS/HTML | OK | grep négatif + QA-19 cas 2 |
| Joueur — `initPlayerHome()` | Présente | OK | player-home.js ligne 27 |
| Joueur — `showPlayerRadar()` | Présente | OK | player-home.js ligne 850 |
| Joueur — `pRecapTableHTML()` | Présente, signature inchangée | OK | player-home.js ligne 774 |
| Joueur — `pRenderTrendTable()` (ex-`pTrendTableHTML`) | Présente ; fonction de rendu appelant `trendTableHTML` (app.js ligne 127) | OK | player-home.js ligne 830 |
| Joueur — `pBilanEntretienHTML()` | Nouvelle fonction présente | OK | player-home.js ligne 702 |
| Joueur — `pLevelFromAvg()` | Nouvelle fonction présente | OK | player-home.js ligne 698 |
| HTML `coach.html` — Supabase CDN | Présent | OK | coach.html ligne 47 |
| HTML `coach.html` — Chart.js CDN | Présent | OK | coach.html ligne 48 |
| HTML `coach.html` — PptxGenJS CDN | Présent | OK | coach.html ligne 49 |
| HTML `coach.html` — html2canvas CDN | Présent | OK | coach.html ligne 50 |
| HTML `coach.html` — jsPDF CDN | Absent (supprimé proprement) | OK | grep négatif + QA-19 cas 4 |
| HTML `player.html` — Supabase CDN | Présent | OK | player.html ligne 40 |
| HTML `player.html` — Chart.js CDN | Présent | OK | player.html ligne 41 |
| HTML `player.html` — Aucun CDN export ajouté | Aucun PptxGenJS, jsPDF, html2canvas | OK | player.html lu intégralement + QA-19 cas 9 |
| CSS `fenix.css` — version bumped | v=50 dans coach.html et player.html | OK | coach.html ligne 16, player.html ligne 16 |
| CSS `fenix.css` — classes `.bilan-*` | 14 classes présentes lignes 1543–1650 | OK | QA-17 cas 11 |

---

## Régressions détectées

Aucune.

---

## Points de surveillance (non bloquants, hérités du QA)

1. **Code mort** : `captureEl()` déclarée en interne dans `exportCoachPPT()` (lignes 615–625) n'est plus appelée depuis la suppression des slides 2 et 5. Pas d'impact runtime. À supprimer lors de la prochaine itération touchant `coach-dashboard.js`.

2. **Commentaires de numérotation incorrects** : les commentaires internes `// SLIDE 3 : Détail Attaque` (ligne 787) et `// SLIDE 4 : Détail Défense` (ligne 790) sont désormais faux (ce sont les slides 2 et 3). Impact nul en production, gêne à la maintenance. Correction recommandée au prochain passage.

3. **CLAUDE.md périmé** : trois références à jsPDF subsistent dans le CLAUDE.md (section Stack, CDN scripts, Performance). À corriger par l'Archiviste en fin de cycle.

---

## Conclusion

Les 22 items de la checklist passent. Aucune régression sur les features critiques existantes. Les trois stories (STORY-15, STORY-16, STORY-17) n'ont pas cassé de comportement antérieur. La mise en production peut être autorisée.
